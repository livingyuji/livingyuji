'use client';
import { useState } from 'react';
import { createClient } from '@supabase/supabase-js';
import { useRouter } from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k){setError('Supabase environment variables are missing. Add them to .env.local.');return;}const s=createClient(u,k);const {error}=await s.auth.signInWithPassword({email,password});if(error){setError(error.message);return;}router.push('/admin');}return <main className="admin-shell"><div className="login"><form className="login-box" onSubmit={submit}><div className="admin-brand">LIVINGYUJI / ADMIN</div><h1 style={{fontSize:22,margin:'24px 0'}}>Control Center</h1><p style={{color:'#aaa4b4',lineHeight:1.7}}>Sign in to edit the entire public website.</p><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div>{error&&<div className="error">{error}</div>}<button className="admin-btn primary" style={{width:'100%'}}>SIGN IN</button><a href="/" className="admin-btn" style={{display:'block',textAlign:'center',marginTop:10}}>BACK TO SITE</a></form></div></main>}
